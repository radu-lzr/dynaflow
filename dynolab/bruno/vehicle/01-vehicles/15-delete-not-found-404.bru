meta {
  name: Delete Vehicle Not Found 404
  type: http
  seq: 15
}

delete {
  url: {{vehicleUrl}}/vehicles/{{mx5Id}}
  body: none
  auth: none
}

headers {
  X-User-Id: {{adminId}}
}

tests {
  test("status 404", function() {
    expect(res.status).to.equal(404);
  });
  test("has message", function() {
    expect(res.body.message).to.be.a("string");
  });
}
