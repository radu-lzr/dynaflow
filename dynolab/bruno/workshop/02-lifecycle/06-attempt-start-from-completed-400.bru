meta {
  name: Attempt Start from Completed → 400
  type: http
  seq: 6
}

put {
  url: {{workshopUrl}}/interventions/{{lifecycleInterventionId}}/start
  body: none
  auth: none
}

headers {
  X-User-Id: {{adminId}}
}

tests {
  test("status 400 (cannot start from completed)", function() {
    expect(res.status).to.equal(400);
  });
}
