meta {
  name: Delete Role With User Assignment Succeeds
  type: http
  seq: 11
}

delete {
  url: {{accessUrl}}/roles/00000000-0000-0000-0000-000000000000
  body: none
  auth: none
}

headers {
  X-User-Id: {{adminId}}
}

tests {
  test("status 404", function() {
    expect(res.status).to.equal(404);
  });
  test("has message", function() {
    expect(res.body.message).to.be.a("string");
  });
}
