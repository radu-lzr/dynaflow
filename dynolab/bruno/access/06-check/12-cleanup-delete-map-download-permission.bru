meta {
  name: Cleanup Delete Map Download Permission
  type: http
  seq: 12
}

delete {
  url: {{accessUrl}}/permissions/{{mapDownloadPermId}}
  body: none
  auth: none
}

headers {
  X-User-Id: {{adminId}}
}

tests {
  test("status 200 or 404", function() {
    expect([200, 404]).to.include(res.status);
  });
  test("has message", function() {
    expect(res.body.message).to.be.a("string");
  });
}
