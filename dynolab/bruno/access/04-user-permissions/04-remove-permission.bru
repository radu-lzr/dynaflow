meta {
  name: Remove Permission From User
  type: http
  seq: 4
}

delete {
  url: {{accessUrl}}/user-permissions/{{userPermId}}
  body: none
  auth: none
}

headers {
  X-User-Id: {{adminId}}
}

tests {
  test("status 200", function() {
    expect(res.status).to.equal(200);
  });
  test("has message", function() {
    expect(res.body.message).to.be.a("string");
  });
}
