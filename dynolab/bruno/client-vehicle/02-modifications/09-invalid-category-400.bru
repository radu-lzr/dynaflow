meta {
  name: Add Modification Invalid Category - 400
  type: http
  seq: 9
}

post {
  url: {{clientVehicleUrl}}/client-vehicles/{{vehicleId}}/modifications
  body: json
  auth: none
}

headers {
  X-User-Id: {{adminId}}
}

body:json {
  {
    "category": "invalid_category",
    "description": "Something invalid"
  }
}

tests {
  test("status 400", function() {
    expect(res.status).to.equal(400);
  });
}
