meta {
  name: Verify Deleted Map Returns 404
  type: http
  seq: 9
}

get {
  url: {{mapUrl}}/maps/{{mapBmwE85Id}}
  body: none
  auth: none
}

headers {
  X-User-Id: {{adminId}}
}

tests {
  test("status 404", function() {
    expect(res.status).to.equal(404);
  });
}
