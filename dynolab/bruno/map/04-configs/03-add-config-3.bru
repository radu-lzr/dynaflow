meta {
  name: Add Compatible Config 3
  type: http
  seq: 3
}

post {
  url: {{mapUrl}}/maps/{{mapGtiStage1Id}}/configs
  body: json
  auth: none
}

headers {
  X-User-Id: {{adminId}}
}

body:json {
  {
    "configId": "eeeeeeeeeeeeeeeeeeeeeeee"
  }
}

tests {
  test("status 201", function() {
    expect(res.status).to.equal(201);
  });
}
