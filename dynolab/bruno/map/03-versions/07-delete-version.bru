meta {
  name: Delete Version 1.1
  type: http
  seq: 7
}

delete {
  url: {{mapUrl}}/maps/{{mapGtiStage1Id}}/versions/{{versionId2}}
  body: none
  auth: none
}

headers {
  X-User-Id: {{adminId}}
}

tests {
  test("status 200", function() {
    expect(res.status).to.equal(200);
  });
  test("has message", function() {
    expect(res.body.message).to.be.a("string");
  });
}
