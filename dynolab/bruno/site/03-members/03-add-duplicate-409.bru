meta {
  name: Add Duplicate Member 409
  type: http
  seq: 3
}

post {
  url: {{siteUrl}}/sites/{{memberSiteId}}/members
  body: json
  auth: none
}

headers {
  X-User-Id: {{adminId}}
}

body:json {
  {
    "userId": "{{techId}}"
  }
}

tests {
  test("status 409", function() {
    expect(res.status).to.equal(409);
  });
  test("has message", function() {
    expect(res.body.message).to.be.a("string");
  });
}
