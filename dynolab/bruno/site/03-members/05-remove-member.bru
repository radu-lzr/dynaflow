meta {
  name: Remove Member
  type: http
  seq: 5
}

delete {
  url: {{siteUrl}}/sites/{{memberSiteId}}/members/{{techId}}
  body: none
  auth: none
}

headers {
  X-User-Id: {{adminId}}
}

tests {
  test("status 200", function() {
    expect(res.status).to.equal(200);
  });
  test("has message", function() {
    expect(res.body.message).to.be.a("string");
  });
}
