meta {
  name: Failed Login Attempt 4/5
  type: http
  seq: 5
}

post {
  url: {{authUrl}}/auth/login
  body: json
  auth: none
}

body:json {
  {
    "email": "{{lockmeEmail}}",
    "password": "wrongpassword"
  }
}

tests {
  test("status 401 or 423", function() {
    expect([401, 423]).to.include(res.status);
  });
}
