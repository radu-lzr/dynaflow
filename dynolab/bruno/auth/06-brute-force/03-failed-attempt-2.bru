meta {
  name: Failed Login Attempt 2/5
  type: http
  seq: 3
}

post {
  url: {{authUrl}}/auth/login
  body: json
  auth: none
}

body:json {
  {
    "email": "lockme@dynolab.com",
    "password": "wrongpassword"
  }
}

tests {
  test("status 401", function() {
    expect(res.status).to.equal(401);
  });
}
