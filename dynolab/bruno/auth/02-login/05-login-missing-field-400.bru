meta {
  name: Login Missing Password Field (400)
  type: http
  seq: 5
}

post {
  url: {{authUrl}}/auth/login
  body: json
  auth: none
}

body:json {
  {
    "email": "{{adminEmail}}"
  }
}

tests {
  test("status 400", function() {
    expect(res.status).to.equal(400);
  });
}
