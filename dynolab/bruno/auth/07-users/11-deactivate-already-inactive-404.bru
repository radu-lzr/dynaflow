meta {
  name: Deactivate Already Inactive User (404)
  type: http
  seq: 11
}

delete {
  url: {{authUrl}}/users/{{clientId}}
  body: none
  auth: none
}

headers {
  X-User-Id: {{adminId}}
}

tests {
  test("status 404", function() {
    expect(res.status).to.equal(404);
  });
  test("has message", function() {
    expect(res.body.message).to.be.a("string");
  });
}
