meta {
  name: Verify No Token (401)
  type: http
  seq: 3
}

post {
  url: {{authUrl}}/auth/verify
  body: none
  auth: none
}

tests {
  test("status 401", function() {
    expect(res.status).to.equal(401);
  });
  test("has message", function() {
    expect(res.body.message).to.be.a("string");
  });
}
