loki:
  auth_enabled: false

  commonConfig:
    replication_factor: 1

  schemaConfig:
    configs:
      - from: 2024-04-01
        store: tsdb
        object_store: s3
        index:
          prefix: index_
          period: 24h

  storage:
    type: s3

minio:
  enabled: true

deploymentMode: Monolithic
singleBinary:
  replicas: 1
  resources:
    requests:
      cpu: 50m
      memory: 128Mi
    limits:
      memory: 512Mi
  persistence:
    enabled: true
    size: 20Gi
    storageClass: standard
backend:
  replicas: 0
read:
  replicas: 0
write:
  replicas: 0